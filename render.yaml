services:
  - type: web
    name: insider-threat-detection
    runtime: python-3.12
    plan: free
    buildCommand: "pip install -r requirements.txt && python -m src.train --epochs 3 --out artifacts/model.joblib"
    envVars:
      - key: PYTHONPATH
        value: src
